# 知识图谱可视化功能说明

## 📊 功能概述

在知识库页面新增了"图谱视图"标签页,可以可视化展示医疗实体(疾病、症状、药品等)之间的关联关系。

## 🎯 主要特性

### 1. 双视图模式
- **搜索浏览**: 传统的列表搜索模式
- **图谱视图**: 新增的关系图谱可视化模式

### 2. 图谱功能
- **关系深度控制**: 支持1层(直接关系)和2层(间接关系)
- **交互式操作**: 
  - 拖拽节点
  - 缩放视图
  - 平移画布
- **节点点击**: 点击节点可切换到该实体的图谱
- **导出图片**: 可将当前图谱导出为PNG图片

### 3. 视觉设计
- **节点颜色区分**:
  - 🔵 疾病 - 蓝色
  - 🟠 症状 - 橙色
  - 🟣 药品 - 紫色
  - 🟢 检查 - 绿色
- **中心节点**: 查询的实体显示为圆形,更大更醒目
- **关系标签**: 连线上显示关系类型

## 🛠️ 技术实现

### 后端 API
新增接口: `GET /api/v1/knowledge/graph/{entity_name}`

**参数:**
- `entity_name`: 实体名称
- `entity_type`: 实体类型(可选)
- `depth`: 关系深度(1-2)

**返回:**
```json
{
  "status": "success",
  "nodes": [...],
  "links": [...]
}
```

### 前端组件
- `KnowledgeGraph.vue`: 图谱可视化组件
- 使用 `relation-graph-vue3` 库实现

### 数据流
```
用户输入实体名称
    ↓
调用后端 API
    ↓
Neo4j 查询关系数据
    ↓
转换为图谱格式
    ↓
RelationGraph 渲染
```

## 📝 使用方法

### 1. 启动服务

**后端:**
```bash
python Agent.py
```

**前端:**
```bash
启动前端.bat
```
或
```bash
cd frontend
npm run dev
```

### 2. 访问图谱

1. 打开浏览器访问 `http://localhost:5173`
2. 登录系统
3. 点击导航栏的"知识库"
4. 切换到"图谱视图"标签
5. 输入实体名称(如"糖尿病")
6. 点击"查看图谱"

### 3. 交互操作

- **查看关系**: 节点之间的连线表示关系,连线上有关系类型标签
- **切换深度**: 使用顶部的"关系深度"下拉框切换1层或2层关系
- **重置视图**: 点击"重置视图"按钮恢复初始视图
- **导出图片**: 点击"导出图片"按钮保存当前图谱
- **点击节点**: 点击任意节点可切换到该节点的关系图谱

## 🎨 示例场景

### 场景1: 查看疾病关系
输入"糖尿病",可以看到:
- 相关症状(多饮、多尿、多食等)
- 推荐药品(二甲双胍、胰岛素等)
- 需要检查(血糖、糖化血红蛋白等)
- 所属科室(内分泌科)
- 并发症(糖尿病肾病、糖尿病视网膜病变等)

### 场景2: 查看症状关联
输入"头痛",可以看到:
- 可能的疾病(感冒、高血压、偏头痛等)
- 相关检查(头颅CT、血压测量等)

### 场景3: 查看药品信息
输入"阿司匹林",可以看到:
- 适用疾病(冠心病、脑梗塞等)
- 相关症状(发热、疼痛等)

## 🔧 配置说明

### 图谱布局
在 `KnowledgeGraph.vue` 中可以调整:
- `defaultNodeWidth`: 节点宽度
- `defaultNodeHeight`: 节点高度
- `layoutName`: 布局算法(center/tree/force等)

### 颜色主题
在 `getNodeColor()` 和 `getBorderColor()` 函数中自定义节点颜色。

## 📦 依赖包

- `relation-graph-vue3`: Vue3 关系图谱组件
- 已在 `frontend/package.json` 中配置

## 🐛 故障排查

### 问题1: 图谱不显示
- 检查后端 Neo4j 服务是否启动
- 检查实体名称是否存在于数据库
- 查看浏览器控制台错误信息

### 问题2: 节点重叠
- 点击"重置视图"按钮
- 尝试切换关系深度
- 手动拖拽节点调整位置

### 问题3: 关系过多
- 降低关系深度(从2层改为1层)
- 后端已限制二度关系最多20个

## 🚀 未来优化

1. **性能优化**: 大规模图谱的渲染优化
2. **布局算法**: 支持更多布局方式(力导向、树状等)
3. **筛选功能**: 按关系类型筛选显示
4. **路径查询**: 查找两个实体之间的最短路径
5. **图谱编辑**: 支持在线编辑关系(管理员功能)

## 📚 参考资料

- [relation-graph 官方文档](https://www.relation-graph.com/)
- [Neo4j 图数据库文档](https://neo4j.com/docs/)
- [Vue3 官方文档](https://vuejs.org/)
