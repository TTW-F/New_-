# 后台管理功能说明

## 功能概述

后台管理系统提供了完整的系统监控和管理功能，只有管理员账号才能访问。

## 访问方式

### 1. 创建管理员账号

运行以下批处理文件创建管理员账号：

```bash
通过API创建管理员.bat
```

或者：

```bash
创建管理员.bat
```

### 2. 登录系统

使用管理员账号登录系统（默认：admin / admin123）

### 3. 访问后台

- 开发环境：http://localhost:5173/admin
- 生产环境：http://你的域名/admin
- 或者点击导航栏右上角的"系统管理"菜单

## 功能模块

### 1. 系统概览 (Dashboard)

显示系统的实时统计数据：

- 总用户数 / 今日新增用户
- 对话总数 / 今日对话数
- 知识库实体数量
- 系统运行状态
- 用户增长趋势图
- 对话活跃度图表

### 2. 用户管理

功能：
- 查看所有用户列表
- 搜索用户（按用户名或邮箱）
- 查看用户详情（包括对话数、反馈数等统计）
- 分页浏览用户数据

用户信息包括：
- 用户ID、用户名、邮箱
- 用户类型（管理员/医生/患者）
- 账号状态（正常/禁用）
- 注册时间、更新时间

### 3. 对话记录

功能：
- 查看所有用户的对话记录
- 按时间过滤（全部/今日/本周）
- 查看对话详情
- 分页浏览对话数据

对话信息包括：
- 对话ID、会话ID
- 用户名
- 对话标题（第一条问题）
- 消息数量
- 创建时间

### 4. 系统日志

功能：
- 查看系统运行日志
- 按级别过滤（全部/信息/警告/错误）
- 实时监控系统状态

日志信息包括：
- 日志级别
- 日志消息
- 时间戳

## API 接口

### 统计数据
```
GET /admin/stats
```

### 用户管理
```
GET /admin/users?search=&page=1&page_size=20
GET /admin/users/{user_id}
```

### 对话记录
```
GET /admin/conversations?filter_type=all&page=1&page_size=20
GET /admin/conversations/{conversation_id}
```

### 系统日志
```
GET /admin/logs?level=all&page=1&page_size=50
```

## 权限控制

- 所有后台接口都需要管理员权限
- 前端路由守卫会检查用户类型
- 后端 API 会验证 JWT Token 和用户角色
- 非管理员用户访问后台会被重定向到首页

## 技术实现

### 后端
- FastAPI 路由：`api/routers/admin.py`
- 权限验证：`require_admin` 依赖注入
- 数据查询：使用 SQLAlchemy ORM

### 前端
- 页面组件：`frontend/src/views/AdminView.vue`
- API 调用：`frontend/src/api/admin.ts`
- 路由配置：`frontend/src/router/index.ts`

## 注意事项

1. 确保数据库已初始化
2. 确保已创建管理员账号
3. 后台页面使用真实 API 数据，不再是 mock 数据
4. 系统日志功能目前返回示例数据，可以扩展为读取实际日志文件
